// string revertido.
// spread operator
// reverse function() solo trabaja en arrays.
// y join al final para unir los strings del array invertido.
const stringRevertido = (str) => {
	return [...str].reverse().join("");
};
